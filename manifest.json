{
  "id": "musetalk",
  "name": "LipSync Image|Video",
  "version": "0.0.6",
  "enabled": true,
  "nav": null,
  "dashboards": [
    {
      "id": "photoreal-face",
      "label": "PhotorealFace",
      "component": "V12Dashboard",
      "variant": 12,
      "apiBase": "/api/musetalk"
    }
  ],
  "exports": [
    {
      "id": "V12Dashboard",
      "type": "component"
    }
  ],
  "settings": {
    "warmup_on_boot": {
      "type": "bool",
      "default": true,
      "label": "Warm up MuseTalk on JarvYZ startup",
      "description": "Pipes the boot 'Ready.' utterance through the wrapper to preload CUDA kernels + mmcv lazy ops. Adds ~2s to first user utterance latency when off.",
      "affectsBoot": true
    },
    "active_ref": {
      "type": "string",
      "default": "",
      "label": "Active reference",
      "description": "Filename of the reference image/video used by the wrapper. Empty = none configured; upload one via Settings. Persisted across JarvYZ restarts (re-applied to wrapper at boot)."
    }
  },
  "requires": {
    "network": "setup",
    "sensors": []
  }
}
